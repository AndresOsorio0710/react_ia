import { createTheme } from "@mui/material/styles"
import { teal, lightGreen, lightBlue } from "@mui/material/colors"
const theme = createTheme({
    palette: {
        primary: teal,
        secondary: lightBlue
    }
});

export default theme;